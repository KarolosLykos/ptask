version: "2.6"

services:
    ptask:
        container_name: ptask_container
        build:
            context: .
            dockerfile: ./Dockerfile
        ports:
            - "8080:8080"
        restart: always